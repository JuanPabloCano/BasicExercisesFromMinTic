# Métodos

# Upper()
# Lower()
# Capitalize
# Swapcase() copia de la cadena convertida en mayuscula y minuscula, inversa los caracteres
# Title() convierte la cadena en formato título
# Center(longitud["caracter de relleno"]) retorna una copia de la cadena centrada
# ljust(longitud[ "caracter de relleno"]) retorna una copia de la cadena alineada a la izquierda
# rjust(longitud["caracter de relleno"]) retorna la cadena alineada a la derecha
# zfill(longitud) retorna una copia de la cadena rellena de ceros a la izquierda hasta alcanzar la longitud indicada. Hay que hacer la conversión a str
# count(subcadena[posicion inicio, posicion fin]) cuenta cuantas veces hay un caracter en una cadena
# find(subcadena[posicion inicio, posicion fin]) retorna la posición de un elemento en una cadena, si no lo encuentra retorna -1 
# startswith(subcadena[posicion inicio, posicion fin]) retorna True o False según el inicio de la cadena
# endswith(subcadena[posicion inicio, posicion fin]) retorna True o False según el final de la cadena
# isalnum() saber si una cadena es alfanúmerica
# isalpha() saber si una cadena es alfabetica  
# is digit() saber si una cadena es numerica
# islower() saber si una cadena contiene solo minusculas
# 
#
#
#
#
#
#






# Verificar si un mail tiene arroba

""" mail=input("Ingrese su Email: ")
amount=0
x=0
for x in range(len(mail)):
    if mail[x] == "@":  # La x pertenece a la variable que entra en el ciclo for y recorre cada elemento de la variable mail con la función len
        amount+=1
        x+=1
if amount == 1:
    print("Es correcto")
else:
    print("Incorrecto") """

